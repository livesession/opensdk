// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("get")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("sdks")
                .visible_alias("sdk")
                .about("List SDKs, or retrieve one by id")
                .subcommand_negates_reqs(true)
                .arg(
                    Arg::new("id")
                        .help("SDK id; omit to list"),
                )
                .arg(
                    Arg::new("limit")
                        .long("limit")
                        .help("Page size"),
                )
                .subcommand(
                    Command::new("targets")
                        .about("List an SDK's targets")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["get", "sdks"] => handle_get_sdks(ctx, o, cmd_path, m).await,
        ["get", "sdks", "targets"] => handle_get_sdks_targets(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_get_sdks<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/sdks".to_string();
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("limit") {
        query.push(("limit", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_get_sdks_targets<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m.get_one::<String>("sdk-id").map(String::as_str).unwrap_or("");
    let path = format!("/sdks/{}/targets", runtime::path_escape(sdk_id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}
