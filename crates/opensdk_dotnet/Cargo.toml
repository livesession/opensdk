[package]
name = "opensdk_dotnet"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "dotnet SDK emitter — OpenSDK IR -> generated dotnet SDK code (Rust port of @xyd-js/opensdk-dotnet; generated-code emitter, vendored runtime + tests deferred)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
opensdk_cli_common = { path = "../opensdk_cli_common" }
opensdk_core = { path = "../opensdk_core" }

[dev-dependencies]
opensdk_e2e = { path = "../opensdk_e2e" }
